# 🚀 Guía Rápida - Nuevos Componentes UI\n\n## 📖 Cómo usar los componentes mejorados\n\n### 🔘 **Botones Animados**\n\n```dart\n// Botón con gradiente\nGradientButton(\n  text: 'Guardar Producto',\n  icon: Icons.save,\n  onPressed: () => _guardarProducto(),\n  gradient: AppTheme.primaryGradient, // Opcional\n  isExpanded: true, // Para ancho completo\n)\n\n// Botón outlined animado\nAnimatedOutlinedButton(\n  text: 'Cancelar',\n  icon: Icons.cancel,\n  onPressed: () => Navigator.pop(context),\n  borderColor: AppTheme.error,\n)\n\n// FAB mejorado\nAnimatedFloatingActionButton(\n  icon: Icons.add,\n  onPressed: () => _agregarProducto(),\n  isExtended: true,\n  label: 'Nuevo',\n  gradient: AppTheme.goldGradient,\n)\n```\n\n### 📱 **Estados de UI**\n\n```dart\n// Loading con skeleton\nif (isLoading) {\n  return ProductCardSkeleton(count: 5);\n}\n\n// Estado vacío\nEmptyWidget(\n  title: 'No hay productos',\n  message: 'Agrega tu primer producto',\n  icon: Icons.inventory,\n  onAction: () => _agregarProducto(),\n  actionButtonText: 'Agregar Producto',\n)\n\n// Error de conexión\nConnectionErrorWidget(\n  onRetry: () => _recargarDatos(),\n)\n\n// Sin resultados de búsqueda\nNoResultsWidget(\n  searchTerm: _searchQuery,\n  onClearSearch: () => _limpiarBusqueda(),\n)\n```\n\n### 🔄 **Pull-to-Refresh Personalizado**\n\n```dart\nCustomRefreshIndicator(\n  onRefresh: () async {\n    await _cargarDatos();\n  },\n  refreshText: 'Desliza para actualizar',\n  releaseText: 'Suelta para recargar',\n  child: ListView(...),\n)\n```\n\n### 🎭 **Transiciones de Página**\n\n```dart\n// Navegación con transición personalizada\nNavigator.of(context).pushSlideFromRight(\n  ProductDetailScreen(productId: product.id),\n);\n\n// Otras transiciones disponibles:\nNavigator.of(context).pushSlideFromBottom(widget);\nNavigator.of(context).pushFade(widget);\nNavigator.of(context).pushScaleAndFade(widget);\nNavigator.of(context).pushMorphing(widget);\n```\n\n### 🍞 **Sistema de Toast**\n\n```dart\n// Toast de éxito\nCustomToast.success(context, 'Producto guardado exitosamente');\n\n// Toast de error\nCustomToast.error(context, 'Error al guardar producto');\n\n// Toast de advertencia\nCustomToast.warning(context, 'Revisa los datos ingresados');\n\n// Toast personalizado\nCustomToast.show(\n  context,\n  message: 'Acción completada',\n  icon: Icons.check,\n  backgroundColor: AppTheme.primaryGreen,\n  duration: Duration(seconds: 2),\n);\n```\n\n### 🎨 **Usando el Tema**\n\n```dart\n// Colores del tema\nContainer(\n  decoration: BoxDecoration(\n    gradient: AppTheme.primaryGradient,\n    borderRadius: BorderRadius.circular(AppTheme.radiusMedium),\n    boxShadow: AppTheme.createShadow(\n      elevation: AppTheme.elevationMedium,\n      color: AppTheme.primaryGreen,\n      opacity: 0.3,\n    ),\n  ),\n)\n\n// Espaciado consistente\nPadding(\n  padding: EdgeInsets.all(AppTheme.space16),\n  child: Column(\n    children: [\n      Text('Título'),\n      SizedBox(height: AppTheme.space8),\n      Text('Contenido'),\n    ],\n  ),\n)\n\n// Tipografía del tema\nText(\n  'Título Principal',\n  style: Theme.of(context).textTheme.headlineLarge,\n)\n```\n\n### 💀 **Skeleton Loaders**\n\n```dart\n// Skeleton general\nSkeletonLoader(\n  width: 200,\n  height: 20,\n  borderRadius: BorderRadius.circular(4),\n  count: 3,\n)\n\n// Skeleton para productos\nProductCardSkeleton(count: 5)\n\n// Loading personalizado\nCustomLoadingIndicator(\n  size: 50,\n  color: AppTheme.primaryGreen,\n  showPulse: true,\n)\n```\n\n## 🎯 **Patrones Recomendados**\n\n### **Para nuevas pantallas:**\n\n```dart\nclass NuevaPantalla extends StatefulWidget {\n  @override\n  _NuevaPantallaState createState() => _NuevaPantallaState();\n}\n\nclass _NuevaPantallaState extends State<NuevaPantalla>\n    with SingleTickerProviderStateMixin {\n  \n  bool _isLoading = false;\n  late AnimationController _animationController;\n  \n  @override\n  void initState() {\n    super.initState();\n    _animationController = AnimationController(\n      duration: Duration(milliseconds: 300),\n      vsync: this,\n    );\n    _cargarDatos();\n  }\n  \n  Future<void> _cargarDatos() async {\n    setState(() => _isLoading = true);\n    try {\n      // Cargar datos...\n      _animationController.forward();\n    } catch (e) {\n      CustomToast.error(context, 'Error al cargar datos');\n    } finally {\n      setState(() => _isLoading = false);\n    }\n  }\n  \n  @override\n  Widget build(BuildContext context) {\n    return Scaffold(\n      body: _isLoading\n          ? SkeletonLoader(...)\n          : FadeTransition(\n              opacity: _animationController,\n              child: _buildContent(),\n            ),\n    );\n  }\n}\n```\n\n### **Para formularios:**\n\n```dart\nColumn(\n  children: [\n    // Campos del formulario...\n    \n    SizedBox(height: AppTheme.space24),\n    \n    // Botones de acción\n    Row(\n      children: [\n        Expanded(\n          child: AnimatedOutlinedButton(\n            text: 'Cancelar',\n            onPressed: () => Navigator.pop(context),\n          ),\n        ),\n        SizedBox(width: AppTheme.space16),\n        Expanded(\n          child: GradientButton(\n            text: 'Guardar',\n            icon: Icons.save,\n            onPressed: _isValid ? _guardar : null,\n            isLoading: _isSubmitting,\n          ),\n        ),\n      ],\n    ),\n  ],\n)\n```\n\n## 🎨 **Colores Disponibles**\n\n```dart\n// Verdes\nAppTheme.primaryGreen     // #2E7D32\nAppTheme.accentGreen      // #66BB6A  \nAppTheme.darkGreen        // #1B5E20\n\n// Dorados\nAppTheme.primaryGold      // #FFB300\nAppTheme.lightGold        // #FFD54F\nAppTheme.darkGold         // #FF8F00\n\n// Acentos\nAppTheme.accentOrange     // #FF7043\nAppTheme.accentRed        // #E53935\n\n// Estados\nAppTheme.success          // #4CAF50\nAppTheme.warning          // #FF9800\nAppTheme.error            // #E53935\nAppTheme.info             // #2196F3\n\n// Gradientes\nAppTheme.primaryGradient  // Verde principal\nAppTheme.goldGradient     // Dorado\nAppTheme.warmGradient     // Cálido (naranja-dorado)\n```\n\n## 📐 **Espaciado y Medidas**\n\n```dart\n// Espaciado\nAppTheme.space4    // 4px\nAppTheme.space8    // 8px\nAppTheme.space12   // 12px\nAppTheme.space16   // 16px - Más común\nAppTheme.space24   // 24px\nAppTheme.space32   // 32px\n\n// Radios\nAppTheme.radiusSmall   // 8px\nAppTheme.radiusMedium  // 12px - Más común\nAppTheme.radiusLarge   // 16px\nAppTheme.radiusXLarge  // 24px\n\n// Elevaciones\nAppTheme.elevationLow      // 2px\nAppTheme.elevationMedium   // 4px - Más común\nAppTheme.elevationHigh     // 8px\nAppTheme.elevationVeryHigh // 16px\n```\n\n¡Con estos componentes tu app tendrá una interfaz profesional y moderna! 🚀✨\n